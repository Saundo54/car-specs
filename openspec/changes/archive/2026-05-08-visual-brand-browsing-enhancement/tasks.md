## 1. Visual Card Implementation

- [x] 1.1 Update `SearchScreen.tsx` to render `v.image` inside the vehicle cards in the `selectedMake` view.
- [x] 1.2 Update `SearchScreen.module.css` to style the card images (consistent aspect ratio, object-fit).
- [x] 1.3 Verify images load correctly from the local `public/images/vehicles/` directory.
